<?xml version="1.0" encoding="utf-8"?>
<!--
  One day of the amount chart: what was logged, the bar, and the date.

  The bar's height is set in code from the amount, so the value sits above a bar whose size is
  proportional to it. A spacer above pushes short bars down to the baseline.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_weight="1"
    android:gravity="center_horizontal"
    android:orientation="vertical">

    <TextView
        android:id="@+id/textBarValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?attr/textAppearanceLabelSmall"
        android:textColor="?attr/colorPrimary"
        android:textSize="10sp"
        tools:text="12" />

    <Space
        android:id="@+id/spaceBar"
        android:layout_width="match_parent"
        android:layout_height="0dp" />

    <View
        android:id="@+id/viewBar"
        android:layout_width="10dp"
        android:layout_height="0dp"
        android:layout_marginTop="2dp"
        android:background="@drawable/bg_heatmap_day_filled" />

    <TextView
        android:id="@+id/textBarDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textAppearance="?attr/textAppearanceLabelSmall"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:textSize="10sp"
        tools:text="1.9." />

</LinearLayout>
