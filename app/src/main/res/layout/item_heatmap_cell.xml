<?xml version="1.0" encoding="utf-8"?><!--
  One day in the history grid.

  The root carries the month band and fills its whole column so adjacent slots tile without seams;
  the box inside is a fixed square, keeping the grid even across screen widths.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/containerBand"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingVertical="3dp">

    <FrameLayout
        android:id="@+id/viewCell"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_gravity="center">

        <TextView
            android:id="@+id/textDay"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:textAppearance="?attr/textAppearanceLabelSmall"
            android:textSize="11sp"
            tools:text="14" />

    </FrameLayout>

</FrameLayout>
