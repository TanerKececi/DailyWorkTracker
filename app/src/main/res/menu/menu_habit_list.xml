<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_all_habits"
        android:title="@string/all_habits_title"
        app:showAsAction="never" />
    <!-- Hidden unless this is a debug build; see HabitListFragment. -->
    <item
        android:id="@+id/action_seed_sample_data"
        android:title="@string/debug_seed_sample_data"
        android:visible="false"
        app:showAsAction="never" />
</menu>
